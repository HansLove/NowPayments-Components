import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="NOWPayments/Introduction" />

# NOWPayments Components

A comprehensive React component library for cryptocurrency payments powered by the NOWPayments API.

## Overview

This library provides ready-to-use React components for building cryptocurrency payment interfaces. It includes everything you need to integrate crypto payments into your application with minimal configuration.

## Key Features

### 🚀 **Easy Integration**
- Pre-built modal components for deposits and withdrawals
- Frontend-only currency fetching from NOWPayments API
- Flexible backend integration with fixed form schemas

### 🎨 **Highly Customizable**
- CSS variables for complete theming control
- Multiple component variants and sizes
- Responsive design for all screen sizes

### ♿ **Accessibility First**
- Full keyboard navigation support
- ARIA labels and semantic markup
- Screen reader compatibility

### 🔧 **Developer Experience**
- TypeScript support with complete type definitions
- Comprehensive Storybook documentation
- Clean API with consistent patterns

## Main Components

### Modals
- **DepositModal** - 3-step cryptocurrency deposit flow
- **WithdrawModal** - Single-form withdrawal interface

### Shared Components
- **Button** - Flexible button with variants and loading states
- **Input** - Material Design-inspired input with floating labels
- **CurrencySelector** - Searchable dropdown for cryptocurrency selection
- **Modal** - Portal-based modal foundation
- **Stepper** - Progress indicator for multi-step processes
- **LoadingSpinner** - Animated loading indicator
- **QRCode** - Cryptocurrency address QR code generator

## Quick Start

```bash
npm install @taloon/nowpayments-components
```

```tsx
import { DepositModal } from '@taloon/nowpayments-components'
import '@taloon/nowpayments-components/styles'

function App() {
  const [isOpen, setIsOpen] = useState(false)

  const handleSubmit = async (formData) => {
    // Your backend integration
    return await fetch('/api/deposits', {
      method: 'POST',
      body: JSON.stringify(formData)
    })
  }

  return (
    <DepositModal
      isOpen={isOpen}
      onClose={() => setIsOpen(false)}
      onSubmit={handleSubmit}
      customerEmail="user@example.com"
    />
  )
}
```

## Architecture

### Frontend Responsibilities
- Currency display and selection
- Form validation and UI states
- User experience and theming

### Backend Integration
- Payment processing via NOWPayments API
- Webhook handling and confirmations
- Business logic and user management

## Theming

The library uses CSS variables for complete customization:

```css
:root {
  --np-primary: #3b82f6;
  --np-secondary: #6b7280;
  --np-surface: #ffffff;
  --np-border: #e5e7eb;
}
```

## Browser Support

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## License

MIT License - see LICENSE file for details.

---

**Explore the components** using the navigation panel to see detailed examples and interactive demos.